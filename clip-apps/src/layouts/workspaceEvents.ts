/** CustomEvent name for menubar → workspace actions (all modes). */
export const WORKSPACE_EVENT = 'shapexcam:workspace'
/** @deprecated use WORKSPACE_EVENT — kept for FDM listeners. */
export const FDM_WORKSPACE_EVENT = WORKSPACE_EVENT