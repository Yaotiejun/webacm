/** Pinned stats for {@link buildTexturizerMediumRegressionVertices} planar XY, amp=1, neutral texture. */
export const TEXTURIZER_MEDIUM_MESH_GOLDEN = Object.freeze({
  vertexCount: 96,
  triangleCount: 32,
  preTriCount: 32,
  postSubdivTriCount: 32,
  postDecimateTriCount: 32,
  minDeltaZ: -0.25,
  maxDeltaZ: 0.25,
})
