/**
 * Laser legacy bootstrap — geo/core resolve via cam/legacy shims.
 * Product path remains laserEngine (ts-mvp); this proves worker imports load.
 */
import '@/core/slicer/kiriLegacyPolyfills'
export { TYPE, FUNC, LASER } from '@/core/laser/legacy/kiri/mode/laser/init-work.js'
